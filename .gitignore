# Dependencies
node_modules/

# Secrets / local env (keep .env.example tracked)
.env
.env.local
.env.*.local
!.env.example

# Signing material (defensive — never commit even if currently outside the repo)
*.keystore
*.jks
*.p8
*.p12
*.pem
*.mobileprovision
key.properties

# Windows setup bundle (contains signing key)
.windows-setup/

# Internal staff uid override (PII — uid↔name roster kept out of git)
scripts/internal-uids.local

# Build
dist/
src-tauri/target/

# IDE
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# iOS build output
src-tauri/gen/apple/build/

# Test results
test-results/

# Debug
npm-debug.log*
pnpm-debug.log*
.android-version-code

# Build / tool caches (machine-generated)
tsconfig.tsbuildinfo
.firebase/

# Local Claude runtime state
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/settings.json

# Local demo/verification artifacts (not for commit)
artifacts/
